# This directory holds the TFLite model file.
#
# On the Raspberry Pi, create it with:
#   mkdir -p /home/pi/edgeai/models
#   touch /home/pi/edgeai/models/model.tflite
#
# The placeholder file is enough for the lab — the script checks
# that the file exists but doesn't actually load it.
# Replace it with your real model when ready.
