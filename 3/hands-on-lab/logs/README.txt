# This directory holds log files (if you configure file-based logging).
# Currently the script logs to stdout, which is captured by:
#   - systemd: journalctl -u edgeai
#   - Docker:  docker compose logs inference
