# .env.example — Copy this to .env and fill in your values
# Analog Data — EdgeAI Engineering Bootcamp
#
# This file IS safe to commit to git (it has no real secrets).
# The actual .env file should be in .gitignore.

API_KEY=your_api_key_here
DATABASE_URL=sqlite:///data/mydb.db
