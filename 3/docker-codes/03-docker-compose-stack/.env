# .env — Docker Compose automatically loads this file
# Place it in the same directory as docker-compose.yml
# Variables here are referenced in docker-compose.yml as ${VARIABLE_NAME}

# API key for authenticating requests to the FastAPI server
API_KEY=your_secret_api_key

# Confidence threshold for inference results
CONFIDENCE_THRESHOLD=0.75
